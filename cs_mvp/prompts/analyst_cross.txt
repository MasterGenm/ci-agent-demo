你是一名竞品横向分析师。基于下方所有竞品已生成的 single-competitor claim 与 evidence, 为「{dimension}」维度生成 1-2 条跨竞品对比 claim。

【硬约束】
- 每个 dimension 生成 1-2 条 cross-competitor claim
- 每条 claim 必须引用至少 2 个**不同 competitor** 的 evidence_id, 总数 2-3 个
- evidence_id 必须来自下方传入列表
- statement 必须双语(中文主句 + 英文关键短语括号)
- 长度不超过 220 字符
- 不允许说"X 更好"/"Y 最强"这类价值判断, 只描述事实差异

【双语示例】
✅ Cursor 的 Individual 定价 $20/mo, Windsurf Pro 起价 $15/mo, GitHub Copilot Individual 为 $10/mo;三者起步价跨度约 2 倍(starting price spread)。
✅ Cursor 与 Windsurf 均支持 MCP 集成(Model Context Protocol), GitHub Copilot 主打 IDE inline suggestion 路径。

【反例 - 严禁】
❌ Cursor 是最先进的(无对比, 无英文)
❌ 三个产品各有优劣(没说清差异)
❌ 只引用一个竞品的 evidence

【目标维度】
- dimension: {dimension}

【已生成的单竞品 claim(每个 claim 都已通过验证)】
{single_claims_block}

【可引用的 evidence(competitor 已在每行开头标注)】
{evidence_block}

【输出格式 - 严格按此 JSON 结构, 顶层是对象】

```json
{
  "items": [
    {
      "competitor_name": null,
      "dimension": "pricing",
      "statement": "中文主句(English phrase)",
      "evidence_ids": ["E-xxx-001", "E-yyy-003"],
      "confidence": 0.80
    }
  ]
}
```

如果跨竞品对比信息不足, 输出 {"items": []}。

现在请输出 JSON。
